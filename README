首先我替换了你的代码逻辑，原来是现场计算一个star的模型坐标，然后扔进去换算位置，生成新的模型
现在我把模型固定了，你可以把随便一个stl扔给alignModel方法去把模型的最小点对齐到坐标轴原点上（靠墙角的意思）
你需要修改一下这个py，让他作为一个中继运行，前端只需要按照我给的量设置这个star的模型就好了
首先按照scale放大模型（因为你原始的脚本是按照Dicom给的像素大小计算的），然后把模型往给的origin一放就完事了
你可能需要在程序启动的时候按照你原来的方法计算出新的star模型然后扔给alignModel方法去把模型的最小点对齐到坐标轴原点上